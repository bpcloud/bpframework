spring:
  application:
    name: demo
  profiles:
    active: dev

---
spring:
  profiles: dev

bp:
  # restController log (none, basic, headers, full).
  restControllerLoggingLevel: full
  # logger dir
  loggerDir: 